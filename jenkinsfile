pipeline {
    agent any
    
tools {
    terraform 'Default'
}
stages {
  
  
  stage('git') {
    steps {
        
        git 'https://github.com/Axceletron/Jenkins.git'
        sh 'sudo cp /home/osboxes/vars.tf ./'
        sh 'ls'
        sh 'pwd'
        sh 'cat vars.tf'
      // One or more steps need to be included within the steps block.
    }
  }


  stage('init') {
    steps {
        sh 'terraform init'
      // One or more steps need to be included within the steps block.
    }
  }

  stage('deploy') {
    steps {
<<<<<<< HEAD
      sh 'echo "Destroying infra"'
      sh 'terraform destroy -auto-approve'
=======
      sh 'terraform apply -auto-approve'
>>>>>>> d4f419ccf0457ef72dab227e727bade3fbf36726
      // One or more steps need to be included within the steps block.
    }
  }

}
}